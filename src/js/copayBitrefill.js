'use strict';

var module = angular.module('copayAddon.bitrefill', ['copayBitrefill.views']);

module
    .config(function ($stateProvider) {

    })
    .run(function (addonManager, coloredCoins, $state) {

    });